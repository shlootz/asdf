package controller 
{
	import citrus.input.controllers.starling.VirtualButton;
	import starling.display.Quad;
	/**
	 * ...
	 * @author Alex Popescu
	 */
	public class MyButton extends VirtualButton
	{
		
		public function MyButton(name:String, params:Object=null) 
		{
			super(name, params);
		}
		
	}

}